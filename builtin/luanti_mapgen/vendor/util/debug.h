// A shim, not Luanti's: a file under util/ that includes "debug.h"
// finds this, and this finds the one beside its own directory.
// See ../README.txt.
#ifndef LUANTI_SHIM_UTIL_DEBUG_H
#define LUANTI_SHIM_UTIL_DEBUG_H
#include "../debug.h"

#endif
